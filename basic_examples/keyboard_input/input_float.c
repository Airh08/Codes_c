#include <stdio.h>
#include<stdlib.h>

int main(){
    float testInput; //local variable and float type that will be assignerd the input
    printf("Enter an number: ");
    scanf("%f", &testInput);
    printf("Number= %f", testInput, "\n");
    system("pause");
}