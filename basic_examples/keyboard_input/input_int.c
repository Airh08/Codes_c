#include <stdio.h>

int main(){
    int testInput; //local variable and integer type that will be assignerd the input
    printf("Enter an integer: ");
    scanf("%d", &testInput);
    printf("Number= %d", testInput);
    return 0;
}